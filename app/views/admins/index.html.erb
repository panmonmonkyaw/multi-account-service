<h1>Admin List</h1>

<table>
  <thead>
    <tr>
      <th>email</th>
      <!-- <th>password</th>
      <th>password confirmation</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
   <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.email %></td>
        <!-- <td><%= admin.password_hash %></td>
        <td><%= admin.password_salt %></td> -->
      <!-- <td><%= link_to 'Show', admin %></td>  
      <td><%= link_to 'Edit', edit_admin_path(admin) %></td>
      <td><%= link_to 'Destroy', edit_admin_path(admin), :method => :delete, data: { confirm: 'Are you sure?' } %></td> -->
       <td><%= link_to 'Destroy', admin, method: :delete, data: { confirm: 'Are you sure?' } %></td> 
      

      
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'sign up', sign_up_path %>
<%= link_to 'log in', log_in_path %>
